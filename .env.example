# TX Pay Frontend - Environment Configuration
# Copy this file to .env.local and fill with your values

# API URL - Backend endpoint
VITE_API_URL=http://localhost:8080

# Default country for Vita Wallet transactions
# Used as fallback when no country is selected
VITE_DEFAULT_COUNTRY=CL
